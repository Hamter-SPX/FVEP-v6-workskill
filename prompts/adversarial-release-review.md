# Adversarial Release Review Prompt

Assume the release can fail despite green local tests. Challenge critical-flow coverage, denied authorization paths, duplicate delivery, concurrency, migrations, rollback, dependency failure, overload, observability, evidence freshness, baseline governance, and build identity. Find the easiest way each gate could be gamed or bypassed. Approve only when hard failures are absent and both quality and evidence confidence meet policy.
